name: ARCHIVE V8.2 — NE PAS TELECHARGER

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive-info:
    runs-on: ubuntu-latest
    steps:
      - name: Information
        run: |
          echo "Ce workflow V8.2 est archivé et ne compile plus automatiquement."
          echo "Utiliser le workflow : ✅ APK UNIQUE V9.5 Zero-Copy — TELECHARGER ICI"
